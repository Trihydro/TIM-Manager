ORACLE_DSN	dbi:Oracle:host={ORACLE_DB_HOST};sid={ORACLE_DB_NAME};port={ORACLE_DB_PORT}
ORACLE_USER	{ORACLE_DB_USERNAME}
ORACLE_PWD	{ORACLE_DB_PASSWORD}
USER_GRANTS     1
TYPE		INSERT
ALLOW PATH
TRUNCATE_TABLE	0
WHERE path_id IN (SELECT path_id FROM region WHERE data_frame_id IN (SELECT data_frame_id FROM data_frame WHERE tim_id IN (SELECT tim_id FROM active_tim)))
DISABLE_SEQUENCE	1
DISABLE_TRIGGERS 1
PG_VERSION  {PG_VERSION}
